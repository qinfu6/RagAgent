(ragbaseline)
import os
import torch
from pathlib import Path
from transformers import AutoTokenizer, AutoModelForCausalLM, pipeline

# 引入截图对应的 LangChain 分块器
from langchain_text_splitters import MarkdownHeaderTextSplitter, RecursiveCharacterTextSplitter
from langchain_huggingface import HuggingFaceEmbeddings, HuggingFacePipeline 
from langchain_community.vectorstores import FAISS
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
from langchain_core.runnables import RunnablePassthrough

from benchmark import Benchmark

result_path = "./result/baseline"

# --- 1. 向量化模型初始化 ---
embeddings = HuggingFaceEmbeddings(
    model_name="./models/Qwen3-Embedding-4B",
    model_kwargs={'device': 'cpu'},  # 使用 CPU，显存不足使用 GPU 会导致结果出错
    encode_kwargs={'normalize_embeddings': True}
)

# --- 2. 向量库构建与加载（完全基于截图的两阶段分块逻辑） ---
persist_dir = "./faiss_index/baseline"

if os.path.exists(persist_dir) and os.listdir(persist_dir):
    vectorstore = FAISS.load_local(
        persist_dir, embeddings, allow_dangerous_deserialization=True
    )
    print("已加载已有向量库")
else:
    os.makedirs(persist_dir, exist_ok=True)
    data_dir = "./doc/3"
    print(f"正在读取 {data_dir} 目录下的 Markdown 文件并进行两阶段切分...")

    # 截图步骤 1：定义分割规则，按标题层级映射
    headers_to_split_on = [
        ("#", "Header_1"),
        ("##", "Header_2"),
        ("###", "Header_3"),
    ]
    markdown_splitter = MarkdownHeaderTextSplitter(headers_to_split_on=headers_to_split_on)

    # 截图步骤 2：定义细分规则（小块大小与重叠度）
    child_text_splitter = RecursiveCharacterTextSplitter(
        chunk_size=400,        # 对应截图中的符合 chunk_size 要求的小块
        chunk_overlap=80,
        length_function=len
    )

    final_chunks = []

    # 扫描并处理所有 Markdown 文件
    for filepath in sorted(Path(data_dir).rglob("*.md")):
        file_name = filepath.name
        with open(filepath, "r", encoding="utf-8") as f:
            md_content = f.read()
        
        # 第一阶段：按标题切分成带有元数据的逻辑块
        macro_chunks = markdown_splitter.split_text(md_content)
        
        # 第二阶段：对这些逻辑块再应用细切，自动继承来自第一步的标题元数据
        for m_chunk in macro_chunks:
            # 额外手动注入文件名，完善你的精确定位需求
            m_chunk.metadata["source"] = file_name
            
            # 使用 split_documents 可以让细切后的小块自动继承 m_chunk.metadata
            micro_chunks = child_text_splitter.split_documents([m_chunk])
            final_chunks.extend(micro_chunks)

    print(f"切分完成：共生成带有丰富结构元数据的小块 {len(final_chunks)} 个")

    # 构建 FAISS 并保存
    vectorstore = FAISS.from_documents(final_chunks, embeddings)
    vectorstore.save_local(persist_dir)
    print("向量库已创建并保存")


# --- 3. 准备生成模型 ---
model_name_or_path = "./models/Qwen3.5-4B"
tokenizer = AutoTokenizer.from_pretrained(model_name_or_path)
model = AutoModelForCausalLM.from_pretrained(
    model_name_or_path,
    device_map="cpu",  
    torch_dtype=torch.float32
)

pipe = pipeline(
    "text-generation",
    model=model,
    tokenizer=tokenizer,
    max_new_tokens=256,
    do_sample=False,
    temperature=0.1,
    return_full_text=False  
)
llm = HuggingFacePipeline(pipeline=pipe)


# --- 4. 格式化检索输出（完美对接元数据展示） ---
def format_docs(docs):
    context_blocks = []
    for doc in docs:
        # 从小块继承的元数据中提取标题路径
        h1 = doc.metadata.get("Header_1", "")
        h2 = doc.metadata.get("Header_2", "")
        h3 = doc.metadata.get("Header_3", "")
        source = doc.metadata.get("source", "")
        
        # 拼接出精准的“地址”路径
        header_path = " -> ".join([h for h in [h1, h2, h3] if h])
        meta_info = f"【来源: {source} | 章节: {header_path}】" if header_path else f"【来源: {source}】"
        
        context_blocks.append(f"{meta_info}\n{doc.page_content}")
        
    context_str = "\n\n---\n\n".join(context_blocks)
    
    print("======== 检索到的结构化上下文 ========")
    print(context_str)
    print("=====================================\n")
    return context_str


# --- 5. 构建 RAG 链 ---
system_prompt = """你是一个严格基于《学生手册》知识库的问答助手。你必须遵守以下铁律：

【绝对基于上下文】
1. 所有回答只能来自提供的【上下文】，不得使用任何外部知识或常识。
2. 如果上下文没有相关信息，必须直接回复：“抱歉，我无法根据提供的上下文找到相关信息来回答此问题。”，禁止补充任何猜测、建议或解释。

【事实精准】
3. 答案中的数字、日期、专有名词必须与上下文原文完全一致，不得改写。
4. 答案末尾必须用括号注明依据来源，格式为：（参见第X条/第X页/某章节），如上下文无明确编号可写（依据原文）。

【忠实保留限制条件】
5. 必须完整保留原文中的所有限制词，如“仅限”“除……之外”“但是”“不得”“必须”“可以”等，严禁改变约束强度和语义方向。例如，不能将“可以”改为“必须”，不能将“不得”改为“原则上不建议”。

【复杂问题的证据链】
6. 当问题需要结合上下文中的多个条款或条件时，你必须先明确列出每一个相关条款的依据，再给出最终结论，确保逻辑桥接清晰，无关键证据遗漏。但总体回答仍应简洁，用“依据1: ...；依据2: ...；结论：...”的格式。
7. 禁止只凭局部信息直接给出结论，必须覆盖所有相关条件。

【输出格式】
- 先直接给出答案（一句话），然后换行后附上“依据：”和简要引用。
- 如果拒答，只需回复拒答语句，不得包含任何其他内容。"""

user_prompt = """上下文:
{context}

问题: {question}

回答:"""

prompt = ChatPromptTemplate.from_messages([
    ("system", system_prompt),
    ("user", user_prompt)
])

rag_chain = (
    {"context": vectorstore.as_retriever(search_kwargs={"k": 4}) | format_docs, "question": RunnablePassthrough()}
    | prompt
    | llm
    | StrOutputParser()
)

def get_answer(query):
    return rag_chain.invoke(query)


if __name__ == "__main__":
    # 第一次运行此改良代码前，请记得手动删除旧的无结构 ./faiss_index/baseline 文件夹，以便重新触发两阶段建库。
    bm = Benchmark(get_answer, result_path)
bm.run()


(Rag-agent)
import os
import re
import torch
from pathlib import Path
from transformers import AutoTokenizer, AutoModelForCausalLM, pipeline

# 引入截图对应的 LangChain 分块器
from langchain_text_splitters import MarkdownHeaderTextSplitter, RecursiveCharacterTextSplitter
from langchain_huggingface import HuggingFaceEmbeddings, HuggingFacePipeline 
from langchain_community.vectorstores import FAISS
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
from langchain_core.runnables import RunnablePassthrough

from benchmark import Benchmark

result_path = "./result/agent"

# --- 1. 向量化模型初始化 ---
embeddings = HuggingFaceEmbeddings(
    model_name="./models/Qwen3-Embedding-4B",
    model_kwargs={'device': 'cpu'},  # 使用 CPU，显存不足使用 GPU 会导致结果出错
    encode_kwargs={'normalize_embeddings': True}
)

# --- 2. 向量库构建与加载（完全基于截图的两阶段分块逻辑） ---
persist_dir = "./faiss_index/agent"

if os.path.exists(persist_dir) and os.listdir(persist_dir):
    vectorstore = FAISS.load_local(
        persist_dir, embeddings, allow_dangerous_deserialization=True
    )
    print("已加载已有向量库")
else:
    os.makedirs(persist_dir, exist_ok=True)
    data_dir = "./doc/3"
    print(f"正在读取 {data_dir} 目录下的 Markdown 文件并进行两阶段切分...")

    # 截图步骤 1：定义分割规则，按标题层级映射
    headers_to_split_on = [
        ("#", "Header_1"),
        ("##", "Header_2"),
        ("###", "Header_3"),
    ]
    markdown_splitter = MarkdownHeaderTextSplitter(headers_to_split_on=headers_to_split_on)

    # 截图步骤 2：定义细分规则（小块大小与重叠度）
    child_text_splitter = RecursiveCharacterTextSplitter(
        chunk_size=400,        # 对应截图中的符合 chunk_size 要求的小块
        chunk_overlap=80,
        length_function=len
    )

    final_chunks = []

    # 扫描并处理所有 Markdown 文件
    for filepath in sorted(Path(data_dir).rglob("*.md")):
        file_name = filepath.name
        with open(filepath, "r", encoding="utf-8") as f:
            md_content = f.read()
        
        # 第一阶段：按标题切分成带有元数据的逻辑块
        macro_chunks = markdown_splitter.split_text(md_content)
        
        # 第二阶段：对这些逻辑块再应用细切，自动继承来自第一步的标题元数据
        for m_chunk in macro_chunks:
            # 手动注入文件名元数据，供后续追溯
            m_chunk.metadata["source"] = file_name
            
            # 使用 split_documents 让切分后的小块自动继承元数据
            micro_chunks = child_text_splitter.split_documents([m_chunk])
            final_chunks.extend(micro_chunks)

    print(f"切分完成：共生成带有丰富结构元数据的小块 {len(final_chunks)} 个")

    # 构建 FAISS 并保存
    vectorstore = FAISS.from_documents(final_chunks, embeddings)
    vectorstore.save_local(persist_dir)
    print("向量库已创建并保存")


# --- 3. 准备生成模型 (使用 HuggingFacePipeline 包装) ---
model_name_or_path = "./models/Qwen3.5-4B"
tokenizer = AutoTokenizer.from_pretrained(model_name_or_path)
model = AutoModelForCausalLM.from_pretrained(
    model_name_or_path,
    device_map="cpu",  # 使用 CPU，显存不足使用 GPU 会导致结果出错
    torch_dtype=torch.float32
)

# 创建一个 transformers pipeline
pipe = pipeline(
    "text-generation",
    model=model,
    tokenizer=tokenizer,
    max_new_tokens=512,
    do_sample=False,
    temperature=0.1,
    return_full_text=False  # 只返回新生成的文本，不包含输入
)

# 使用 LangChain 的 HuggingFacePipeline 包装器
llm = HuggingFacePipeline(pipeline=pipe)


# --- 4. 改良：格式化检索输出（无缝提取并拼装两阶段小块的元数据） ---
def format_docs(docs):
    context_blocks = []
    for doc in docs:
        # 提取当前小块在第一阶段被赋予的标题路径
        h1 = doc.metadata.get("Header_1", "")
        h2 = doc.metadata.get("Header_2", "")
        h3 = doc.metadata.get("Header_3", "")
        source = doc.metadata.get("source", "")
        
        # 拼装结构化路径
        header_path = " -> ".join([h for h in [h1, h2, h3] if h])
        meta_info = f"【来源: {source} | 章节: {header_path}】" if header_path else f"【来源: {source}】"
        
        # 将路径前缀与小块文本结合
        context_blocks.append(f"{meta_info}\n{doc.page_content}")
        
    return "\n\n---\n\n".join(context_blocks)


# 实例化检索器
retriever = vectorstore.as_retriever(search_kwargs={"k": 4})


# --- 5. RAG 链与决策层 Agent 模块 ---
# 提示词模板
system_prompt = """你是一个严格基于《学生手册》知识库的问答助手。你必须遵守以下铁律：

【绝对基于上下文】
1. 所有回答只能来自提供的【上下文】，不得使用任何外部知识或常识。
2. 如果上下文没有相关信息，必须直接回复：“抱歉，我无法根据提供的上下文找到相关信息来回答此问题。”，禁止补充任何猜测、建议或解释。

【事实精准】
3. 答案中的数字、日期、专有名词必须与上下文原文完全一致，不得改写。
4. 答案末尾必须用括号注明依据来源，格式为：（参见第X条/第X页/某章节），如上下文无明确编号可写（依据原文）。

【忠实保留限制条件】
5. 必须完整保留原文中的所有限制词，如“仅限”“除……之外”“但是”“不得”“必须”“可以”等，严禁改变约束强度和语义方向。例如，不能将“可以”改为“必须”，不能将“不得”改为“原则上不建议”。

【复杂问题的证据链】
6. 当问题需要结合上下文中的多个条款或条件时，你必须先明确列出每一个相关条款的依据，再给出最终结论，确保逻辑桥接清晰，无关键证据遗漏。但总体回答仍应简洁，用“依据1: ...；依据2: ...；结论：...”的格式。
7. 禁止只凭局部信息直接给出结论，必须覆盖所有相关条件。

【输出格式】
- 先直接给出答案（一句话），然后换行后附上“依据：”和简要引用。
- 如果拒答，只需回复拒答语句，不得包含任何其他内容。"""

user_prompt = """上下文:
{context}

问题: {question}

回答:"""

# 构建提示词模板
prompt = ChatPromptTemplate.from_messages([
    ("system", system_prompt),
    ("user", user_prompt)
])

# 决策层提示词
decision_template = """你的任务是判断给定的上下文是否能够回答用户的问题。
如果上下文包含回答问题所需的信息，返回 1。
如果上下文不包含相关信息，返回 0。
只返回 0 或 1，不要返回任何其他内容。

上下文: {context}

问题: {question}

判断:"""
decision_prompt = ChatPromptTemplate.from_template(decision_template)

def check_context_relevance(context, question):
    if not context.strip():
        return False
    chain = decision_prompt | llm | StrOutputParser()
    result = chain.invoke({"context": context, "question": question})
    print(f"[决策层agent评分]: {result.strip()}")
    return "1" in result.strip()

# Query 改写层提示词
reformulate_template = """你是一个搜索查询优化助手。
请根据原始问题，提取核心关键词或改写为更适合在文档库中检索的表述。
只返回优化后的检索词，不要返回任何其他内容。

原始问题: {question}

优化检索词:"""
reformulate_prompt = ChatPromptTemplate.from_template(reformulate_template)

def reformulate_query(query):
    chain = reformulate_prompt | llm | StrOutputParser()
    return chain.invoke({"question": query})


# --- 6. 执行核心业务流 ---
def get_answer(query):
    # 第一次检索
    docs = retriever.invoke(query)
    local_context = format_docs(docs)

    # 打印第一次检索出的带结构信息上下文，方便调试观察
    print(f"\n[Agent 流程] 原始检索 Query: '{query}'")
    
    # 评估相关性
    if check_context_relevance(local_context, query):
        print("[决策层agent]: 首次检索出的上下文足够回答，直接生成结果。")
        answer_context = local_context
    else:
        # 首次检索不足以回答，触发 Query 改写
        optimized_query = reformulate_query(query).strip()
        print(f"[决策层agent]: 首次检索缺失关键信息。触发查询优化 -> 新 Query: '{optimized_query}'")
        
        # 用优化后的 Query 进行第二次检索
        docs2 = retriever.invoke(optimized_query)
        answer_context = format_docs(docs2)
    
    # 打印最终入链的上下文
    print("======== 最终送入模型的上下文 ========")
    print(answer_context)
    print("======================================\n")

    # 驱动主链生成最终回答
    chain = prompt | llm | StrOutputParser()
    return chain.invoke({"context": answer_context, "question": query}).strip()


if __name__ == "__main__":
    # 如果要重新体验由两阶段分块构建的高级向量库，建议运行前手动清理 ./faiss_index/agent 目录
    bm = Benchmark(get_answer, result_path)
    bm.run()
